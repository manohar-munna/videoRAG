<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/card_white_bg"
    android:padding="16dp">

    <!-- Header Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="12dp">

        <TextView
            android:id="@+id/tvDialogTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="📋 All Indexed JSON Data"
            android:textColor="@color/text_title"
            android:textSize="16sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnCopyJson"
            android:layout_width="wrap_content"
            android:layout_height="34dp"
            android:background="@drawable/btn_folder_bg"
            android:text="📋 Copy JSON"
            android:textAllCaps="false"
            android:textColor="#2563EB"
            android:textStyle="bold"
            android:textSize="11sp"
            android:paddingHorizontal="10dp" />
    </LinearLayout>

    <TextView
        android:id="@+id/tvJsonCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Total Indexed Keyframes: 0"
        android:textColor="@color/text_muted"
        android:textSize="12sp"
        android:layout_marginBottom="8dp" />

    <!-- Scrollable JSON Content Box -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="360dp"
        android:background="@drawable/telemetry_box_bg"
        android:padding="10dp"
        android:layout_marginBottom="12dp">

        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/tvJsonContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="[\n  // No JSON data indexed yet.\n]"
                android:textColor="@color/text_title"
                android:textSize="11sp"
                android:typeface="monospace"
                android:textIsSelectable="true" />
        </HorizontalScrollView>
    </ScrollView>

    <!-- Close Button -->
    <Button
        android:id="@+id/btnCloseJsonDialog"
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:background="@drawable/btn_primary_bg"
        android:text="Close"
        android:textAllCaps="false"
        android:textColor="#ffffff"
        android:textSize="13sp"
        android:textStyle="bold" />

</LinearLayout>
